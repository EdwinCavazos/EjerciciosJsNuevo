//  Bad practices
var first = 1;
var second = 2;

// Why doesn't it work?
// Es una palabra reservada, no se puede declarar como una variable.
var decision = "yes"; //no puede ir if

// Should equal formula for wild wings
var buffaloWings = "wings" + "buffalo";

// Null variable
// El nombre de la variable no puede ser Null pues es palabra reservada
var nulo = null; //no puede ir null

// All variables should equal 1
var uno = (one = wan = uma = um = 1);

// It's broken
console.log(".1");

// Console.log para mostrar [area] de un cuadrado
ladoA = 8;
ladoB = 8;
console.log(ladoA * ladoB);

// Should equal 4
var four = 2 + 2;

// Doesn't work for math problems
var pi = 3.141516;
